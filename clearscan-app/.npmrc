# Fewer install logs: hides deprecation warnings from transitive deps (e.g. Vercel CLI)
loglevel=warn

# Those warnings (path-match, inflight, glob, uuid, tar) come from @vercel/fun and @vercel/nft.
# Safe to ignore; fixes require updates from the Vercel CLI team.
